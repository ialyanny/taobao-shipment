# 手機導入實測指引 v2（2026-08-15）

v1 實測結論：**iPhone Safari 與 Android Chrome 都無法新增 `javascript:` 書籤**，瀏覽器書籤路被主流瀏覽器封鎖。

v2 改採兩條路：

## 路 A：Android 自動帶入（Firefox 短導入碼）

實測重點：**Firefox 網址列貼上 166 字元導入碼能否執行並完成導入**。

1. Play 商店安裝 **Firefox**（免費）
2. 系統「淘寶訂單導入」卡片 → 下方「Android 手機導入碼」→ 按「**複製導入碼**」
3. 用 **Firefox** 開 `https://main.m.taobao.com/olist/index.html?tabCode=all`，登入淘寶
4. **長按網址列 → 貼上**導入碼 → 前往
5. 觀察：
   - 有沒有跳出「正在讀取淘寶訂單…」狀態框？
   - 有沒有跳出「選擇要導入的包裹」勾選面板？
   - 點「導入選取的包裹」後，有沒有跳回集運系統並顯示「已導入 N 筆」？

## 路 B：iPhone／Android 通用（App 複製單號→系統貼上，已可用）

實測重點：流程順暢度。

1. 淘寶 App 訂單頁 → 複製物流單號（一筆或多筆）
2. 開集運系統 → 「登錄新包裹」卡片 → 「**手機快速導入：貼上物流單號**」欄位
3. 每行一個貼上 → 按「建立包裹」
4. 觀察：單號有沒有掛在登入者名下？已存在的有沒有跳過？

## 回報表

| 手機 | 路 | 結果 | 卡在哪一步 | 備註 |
|---|---|---|---|---|
| iPhone | B | | | |
| Android | A | | | |
| Android | B | | | |

回報後我會定案手機瀏覽器主方案（路 A 若 Firefox 可行即成為 Android 自動帶入方案；iPhone 維持路 B）。